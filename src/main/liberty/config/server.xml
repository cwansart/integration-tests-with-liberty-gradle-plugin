<?xml version="1.0" encoding="UTF-8"?>
<server description="${project.name}">

    <featureManager>
        <feature>javaee-7.0</feature>
    </featureManager>

    <httpEndpoint id="defaultHttpEndpoint"
                  host="*"
                  httpPort="9080"
                  httpsPort="9443"/>

    <webApplication location="gradle-liberty-plugin-for-integration-tests.war" contextRoot="/gradle-liberty-plugin-for-integration-tests"/>
    <mpMetrics authentication="false"/>
</server>
